# get from gitlab: https://docs.gitlab.com/ci/runners/runners_scope/#group-runners
gitlab_runner_token:
postgres_password:
minio_password:
grafana_admin_password:
neo4j_password:
harbor_admin_password:
harbor_registry_password:
harbor_core_secret:
harbor_jobservice_secret:
harbor_registry_secret:
redis_password:
ci_wifi_password:
aws_access_key_id:
aws_secret_access_key:
tailscale_auth_key:
# LocalStack Pro auth token — get from https://app.localstack.cloud/workspace/auth-token
# Required by platform-api's test_cfn_deploy (deploys CFN templates against
# LocalStack Pro to validate Secrets Manager dynamic refs + cross-account
# Lambda layer fetching, both of which need Pro).
localstack_auth_token:
